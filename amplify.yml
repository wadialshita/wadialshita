version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: index.html
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
